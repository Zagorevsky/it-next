import styles from './blocks/Menu.module.css';

const Menu = () => {
  return (
    <section className={ styles.container }>
      <div className={ styles.burger } />
    </section>
  );
};

export default Menu;
